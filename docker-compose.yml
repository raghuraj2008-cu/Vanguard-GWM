version: '3.8'
services:
  vanguard-gwm:
    build: .
    container_name: vanguard_gwm_dashboard
    restart: unless-stopped
    ports:
      - "8501:8501"
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
